<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Lista de Tareas</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h1>Tareas</h1>
        <div th:if="${error}" th:text="${error}" style="color: red;"></div>
        <table class="table">
            <thead class="thead-light">
                <tr>
                    <th>Nombre</th>
                    <th>Descripción</th>
                    <th>Fecha de Inicio</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                <tr th:each="tarea : ${tareas}">
                    <td th:text="${tarea.nombre}">Nombre</td>
                    <td th:text="${tarea.descripcion}">Descripción</td>
                    <td th:text="${tarea.fechaInicio}">Fecha de Inicio</td>
                    <td>
                        <a class="btn btn-warning" th:href="@{/tarea/editar/{id}(id=${tarea.id})}">Editar</a>
                        <a class="btn btn-danger" th:href="@{/tarea/eliminar/{id}(id=${tarea.id})}">Eliminar</a>
                    </td>
                </tr>
            </tbody>
        </table>
        <a class="btn btn-primary" href="/tarea/crear">Crear Nueva Tarea</a>
    </div>
</body>
</html>
