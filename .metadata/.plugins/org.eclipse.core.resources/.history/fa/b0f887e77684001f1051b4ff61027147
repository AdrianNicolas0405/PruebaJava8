<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Lista de Tareas dadas de alta</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa; /* Fondo suave */
            font-family: Arial, sans-serif; /* Fuente más legible */
        }
        h1 {
            margin-top: 20px; /* Espacio arriba del encabezado */
            margin-bottom: 20px; /* Espacio abajo del encabezado */
            color: #343a40; /* Color oscuro */
        }
        .table th, .table td {
            vertical-align: middle; /* Alineación vertical */
        }
        .btn-danger {
            padding: 0.5rem 1rem; /* Espaciado en botones de eliminar */
        }
		.btn-nueva-tarea {
		    width: 200px; /* Ancho del botón */
		    border-radius: 25px; /* Bordes redondeados */
		    padding: 0.5rem 1rem; /* Espaciado */
		    margin-top: 40px; /* Aumentar el espacio arriba del botón */
		}
    </style>
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center">Tareas dadas de alta</h1> <!-- Encabezado centrado -->
        <div th:if="${error}" th:text="${error}" class="alert alert-danger text-center" role="alert"></div>
        <table class="table table-bordered table-hover">
            <thead class="thead-light">
                <tr>
                    <th>Nombre</th>
                    <th>Descripción</th>
                    <th>Fecha de Inicio</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                <tr th:each="tarea : ${tareas}">
                    <td th:text="${tarea.nombre}">Nombre</td>
                    <td th:text="${tarea.descripcion}">Descripción</td>
                    <td th:text="${tarea.fechaInicio}">Fecha de Inicio</td>
                    <td>
                        <a class="btn btn-danger btn-sm" th:href="@{/tarea/eliminar/{id}(id=${tarea.id})}">x</a>
                    </td>
                </tr>
            </tbody>
        </table>
        <div class="text-center"> <!-- Contenedor centrado -->
            <a class="btn btn-primary btn-nueva-tarea" href="/tarea/crear">Dar de Alta Nueva Tarea</a>
        </div>
    </div>
</body>
</html>
